import { FormEvent, useEffect, useMemo, useState } from 'react'
import { Activity, AppWindow, Beaker, BookOpen, Boxes, ChevronRight, CircleCheck, Container, FileArchive, FileText, Gauge, Languages, LogOut, Moon, RadioTower, Rocket, Search, Server, ShieldCheck, Sun, TriangleAlert, Waves } from 'lucide-react'
import { useTranslation } from 'react-i18next'
import { UserManager } from 'oidc-client-ts'
import { Agent, API, Asset, DataLifecycleDomain, DataLifecycleRequest, ObservabilityOverview, Scenario, Service, SyntheticRun } from './api'
import { EntityContext, ObservabilityPage, ObservabilityView } from './Observability'

type Page = 'overview' | 'catalog' | 'tests' | 'releases' | 'agents' | 'governance' | 'help' | ObservabilityPage
const glossary = [
  ['Availability', 'Percentual de tentativas concluídas com sucesso.'], ['Latency p95', '95% das respostas terminam abaixo desse valor.'],
  ['Error rate', 'Proporção de operações que falham.'], ['Throughput', 'Quantidade de trabalho concluído por unidade de tempo.'],
  ['SLO', 'Meta explícita de confiabilidade em uma janela.'], ['Error budget', 'Falha tolerável antes de colocar o SLO em risco.'],
  ['Burn rate', 'Velocidade de consumo do error budget.'], ['Baseline', 'Período ou versão usada como referência.'],
]

export default function App() {
  const { t, i18n } = useTranslation(); const [token, setToken] = useState(() => sessionStorage.getItem('sentinel-token') || '')
  const api = useMemo(() => new API(token), [token]); const oidc=useMemo(()=>oidcManager(),[]); const [page, setPage] = useState<Page>('overview'); const [theme, setTheme] = useState(() => localStorage.getItem('sentinel-theme') || 'dark')
  const [services, setServices] = useState<Service[]>([]); const [agents, setAgents] = useState<Agent[]>([]); const [scenarios, setScenarios] = useState<Scenario[]>([]); const [runs, setRuns] = useState<SyntheticRun[]>([]); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const [refreshedAt, setRefreshedAt] = useState<Date | null>(null); const [refreshNonce,setRefreshNonce]=useState(0); const [catalogQuery,setCatalogQuery]=useState(''); const [entityContext,setEntityContext]=useState<EntityContext>({})
  useEffect(() => { document.documentElement.dataset.theme = theme; localStorage.setItem('sentinel-theme', theme) }, [theme])
  useEffect(()=>{if(token||!oidc||!new URLSearchParams(location.search).has('code'))return;oidc.signinRedirectCallback().then(user=>{history.replaceState({},document.title,location.pathname);sessionStorage.setItem('sentinel-token',user.access_token);setToken(user.access_token)}).catch(e=>setError(String(e)))},[oidc,token])
  useEffect(() => { if (!token) return; setLoading(true); Promise.all([api.services(), api.agents(), api.scenarios(), api.syntheticRuns()]).then(([s,a,sc,runs]) => { setServices(s); setAgents(a); setScenarios(sc); setRuns(runs); setRefreshedAt(new Date()); setError('') }).catch(e => setError(String(e))).finally(() => setLoading(false)) }, [api, token, refreshNonce])
  if (!token) return <Login api={api} oidc={oidc} onLogin={value => { sessionStorage.setItem('sentinel-token', value); setToken(value) }} />
  const nav: [Page, string, typeof Gauge][] = [['overview', t('overview'), Gauge], ['hosts', 'Hosts', Server], ['docker', 'Docker', Container], ['logs', 'Logs', FileText], ['metrics', 'Metrics', Activity], ['apm', 'APM', AppWindow], ['traces', 'Traces', Waves], ['profiles', 'Profiles', Gauge], ['catalog', t('catalog'), Boxes], ['tests', t('tests'), Beaker], ['releases', t('releases'), Rocket], ['agents', t('agents'), RadioTower], ['governance', 'Governança de dados', FileArchive], ['help', t('help'), BookOpen]]
  const logout = () => { sessionStorage.removeItem('sentinel-token'); setToken('');if(oidc)void oidc.signoutRedirect() }
  const controlPlane = error ? 'Control Plane indisponível' : loading ? 'Consultando Control Plane' : refreshedAt ? `Control Plane consultado às ${refreshedAt.toLocaleTimeString('pt-BR')}` : 'Control Plane sem dados'
  return <div className="shell">
    <aside className="sidebar"><div className="brand"><div className="brandmark"><ShieldCheck /></div><div><strong>SentinelOps</strong><small>Delivery Assurance</small></div></div>
      <nav aria-label="Principal">{nav.map(([id,label,Icon]) => <button key={id} className={page===id?'active':''} onClick={() => setPage(id)}><Icon size={19}/><span>{label}</span></button>)}</nav>
      <div className={`sidebar-foot ${error?'unavailable':''}`}><div className="control-plane"><span className="pulse"/>{controlPlane}</div><button onClick={logout}><LogOut size={18}/>{t('logout')}</button></div>
    </aside>
    <main><header className="topbar"><div className="search"><Search size={18}/><input aria-label="Buscar no catálogo" value={catalogQuery} onChange={e=>{setCatalogQuery(e.target.value);setPage('catalog')}} placeholder="Buscar no catálogo"/><kbd>⌘ K</kbd></div><div className="tools"><button aria-label="Atualizar dados" onClick={()=>setRefreshNonce(value=>value+1)} disabled={loading}>Atualizar</button><button aria-label="Alternar idioma" onClick={() => { const next=i18n.language==='pt-BR'?'en-US':'pt-BR'; void i18n.changeLanguage(next);localStorage.setItem('sentinel-language',next) }}><Languages size={19}/></button><button aria-label="Alternar tema" onClick={() => setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?<Sun size={19}/>:<Moon size={19}/>}</button><div className="avatar">SR</div></div></header>
      <section className="content">{error&&<div className="error"><TriangleAlert size={18}/>{error}</div>}{loading&&<div className="loading">Atualizando sinais…</div>}
        {page==='overview'&&<Overview api={api} services={services} agents={agents} scenarios={scenarios} runs={runs} refreshedAt={refreshedAt} t={t}/>} {(['hosts','docker','logs','metrics','apm','traces','profiles'] as Page[]).includes(page)&&<ObservabilityView page={page as ObservabilityPage} api={api} context={entityContext} onContext={setEntityContext} onPage={setPage}/>} {page==='catalog'&&<Catalog api={api} services={services} query={catalogQuery} onQuery={setCatalogQuery}/>} {page==='tests'&&<TestStudio services={services} scenarios={scenarios} api={api} onSaved={s=>setScenarios(x=>[...x.filter(i=>i.name!==s.name),s])}/>} {page==='releases'&&<ReleaseView/>} {page==='agents'&&<Agents agents={agents}/>} {page==='governance'&&<DataGovernance api={api}/>} {page==='help'&&<Help/>}
      </section>
    </main>
  </div>
}

function Login({api,oidc,onLogin}:{api:API;oidc:UserManager|null;onLogin:(token:string)=>void}) { const {t}=useTranslation(); const [error,setError]=useState(''); const [busy,setBusy]=useState(false)
  async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);try{const out=await api.login(String(f.get('username')),String(f.get('password')));onLogin(out.accessToken)}catch(err){setError(String(err))}finally{setBusy(false)}}
  return <div className="login-page"><div className="ambient one"/><div className="ambient two"/><form className="login-card" onSubmit={submit}><div className="brandmark large"><ShieldCheck/></div><p className="eyebrow">OBSERVABILITY · SYNTHETICS · DELIVERY</p><h1>{t('loginTitle')}</h1><p>{oidc?'Autentique-se pelo provedor OIDC corporativo.':<>Use a identidade local gerada por <code>make bootstrap</code>.</>}</p>{error&&<div className="error">{error}</div>}{oidc?<button type="button" className="primary" onClick={()=>void oidc.signinRedirect()}>Continuar com OIDC<ChevronRight size={18}/></button>:<><label>{t('username')}<input name="username" defaultValue="admin" autoComplete="username" required/></label><label>{t('password')}<input name="password" type="password" autoComplete="current-password" required/></label><button className="primary" disabled={busy}>{busy?'Autenticando…':t('login')}<ChevronRight size={18}/></button></>}<small>Sessão curta · MFA delegado ao provedor OIDC em produção</small></form></div>
}

function oidcManager():UserManager|null{const cfg=window.__SENTINEL_CONFIG__;if(cfg?.authMode!=='oidc'||!cfg.oidcAuthority||!cfg.oidcClientId)return null;return new UserManager({authority:cfg.oidcAuthority,client_id:cfg.oidcClientId,redirect_uri:location.origin+'/',post_logout_redirect_uri:location.origin+'/',response_type:'code',scope:cfg.oidcScope||'openid profile sentinelops.api',automaticSilentRenew:true,monitorSession:true})}

function Overview({api,services,agents,scenarios,runs,refreshedAt,t}:{api:API;services:Service[];agents:Agent[];scenarios:Scenario[];runs:SyntheticRun[];refreshedAt:Date|null;t:(k:string)=>string}) { const offline=agents.filter(a=>a.status==='offline').length; const source=refreshedAt?`Catálogo/API em ${refreshedAt.toLocaleString('pt-BR')}`:'Aguardando consulta à API'; const latest=runs[0]; const runStatus=latest?`${latest.status} · ${latest.scenario}`:'Sem dados'; const [observability,setObservability]=useState<ObservabilityOverview>(); useEffect(()=>{let active=true;api.observabilityOverview().then(value=>active&&setObservability(value)).catch(()=>active&&setObservability(undefined));return()=>{active=false}},[api]); const signal=(name:string)=>observability?.values[name]==null?'Sem dados':String(Math.round(observability.values[name]!)); const telemetrySource=observability?.sources.prometheus.state==='available'?'Prometheus consultado':observability?.sources.prometheus.message||'Telemetria não consultada'
  return <><div className="page-head"><div><p className="eyebrow">SENTINEL CONTROL PLANE</p><h1>{t('welcome')}</h1><p>{t('subtitle')}</p></div><span className="time-range">{source}</span></div>
    <div className="metrics"><Metric icon={CircleCheck} label="Hosts UP" value={signal('hostsUp')} detail={telemetrySource}/><Metric icon={Server} label={t('services')} value={signal('services')} detail={telemetrySource}/><Metric icon={Container} label="Containers" value={signal('containers')} detail={telemetrySource}/><Metric icon={RadioTower} label={t('offlineAgents')} value={String(offline)} tone={offline?'bad':''} detail={source}/><Metric icon={Beaker} label={t('activeTests')} value={runStatus} detail={latest?.finishedAt?`evidência: ${new Date(latest.finishedAt).toLocaleString('pt-BR')}`:`${scenarios.filter(s=>s.enabled).length} cenários configurados; sem execução`}/></div>
    <div className="grid two"><section className="panel"><PanelTitle icon={Activity} title="Pulso operacional" subtitle="A disponibilidade exige amostras, janela e fonte."/><Empty text="Sem série de disponibilidade consultada. Configure uma fonte de telemetria e uma janela antes de exibir uma medição."/></section>
      <section className="panel"><PanelTitle icon={Rocket} title="Confiança das entregas" subtitle="Somente gates executados podem aprovar uma release."/><Empty text="Sem resultado de gate vinculado a release, commit e evidência. Nenhum PASS é inferido."/></section></div></>
}
function Metric({icon:Icon,label,value,detail,tone=''}:{icon:typeof Gauge;label:string;value:string;detail?:string;tone?:string}){return <article className={`metric ${tone}`}><div className="metric-icon"><Icon size={20}/></div><p>{label}</p><strong>{value}</strong>{detail&&<small>{detail}</small>}</article>}
function PanelTitle({icon:Icon,title,subtitle}:{icon:typeof Gauge;title:string;subtitle:string}){return <div className="panel-title"><div><Icon size={19}/><h2>{title}</h2></div><p>{subtitle}</p></div>}

function Catalog({api,services,query,onQuery}:{api:API;services:Service[];query:string;onQuery:(value:string)=>void}){const [assets,setAssets]=useState<Asset[]>([]);const [nextCursor,setNextCursor]=useState('');const [loading,setLoading]=useState(false);const [error,setError]=useState('');const normalized=query.toLowerCase();const shown=services.filter(s=>(s.displayName+s.name+s.ownerTeam).toLowerCase().includes(normalized));useEffect(()=>{let active=true;const timer=window.setTimeout(()=>{setLoading(true);api.searchAssets(query).then(result=>{if(active){setAssets(result.items);setNextCursor(result.nextCursor||'');setError('')}}).catch(e=>active&&setError(String(e))).finally(()=>active&&setLoading(false))},250);return()=>{active=false;window.clearTimeout(timer)}},[api,query]);async function loadMore(){if(!nextCursor)return;setLoading(true);try{const result=await api.searchAssets(query,nextCursor);setAssets(current=>[...current,...result.items]);setNextCursor(result.nextCursor||'');setError('')}catch(e){setError(String(e))}finally{setLoading(false)}}return <><div className="page-head"><div><p className="eyebrow">SERVICE & ASSET CATALOG</p><h1>Serviços e ativos sob responsabilidade</h1><p>Inventário por ID estável, origem e lifecycle. Saúde só aparece quando houver evidência própria.</p></div></div><div className="filter"><Search size={18}/><input value={query} onChange={e=>onQuery(e.target.value)} placeholder="Filtrar por nome, ID, tipo, site ou owner"/></div><div className="service-list">{shown.map(s=><article key={s.id} className="service-card"><div className="service-avatar"><AppWindow/></div><div><h2>{s.displayName}</h2><code>{s.name}</code><p>{s.description||'Sem descrição operacional.'}</p></div><div className="service-meta"><span>Tier {s.tier}</span><span>{s.ownerTeam}</span><b>Estado de telemetria: não consultado</b></div><ChevronRight/></article>)}{!shown.length&&<Empty text="Nenhum serviço encontrado. Use sentinelctl service apply."/>}</div><section className="panel"><PanelTitle icon={Server} title="Ativos inventariados" subtitle="Consulta limitada e paginada; lifecycle não representa saúde operacional."/>{error&&<div className="error">{error}</div>}<div className="agent-table"><div className="table-head"><span>Ativo</span><span>Tipo / site</span><span>Owner</span><span>Origem</span><span>Lifecycle</span></div>{assets.map(a=><div className="table-row" key={a.id}><div><b>{a.name}</b><br/><code>{a.assetId}</code></div><span>{a.kind+(a.site?' · '+a.site:'')}</span><span>{a.ownerTeam||'Não definido'}</span><span>{a.source}</span><strong className={a.lifecycle}><i/>{a.lifecycle}</strong></div>)}{!loading&&!assets.length&&<Empty text="Nenhum ativo encontrado para esta consulta."/>}</div>{loading&&<div className="loading">Consultando catálogo…</div>}{nextCursor&&<button className="primary compact" onClick={()=>void loadMore()} disabled={loading}>Carregar mais ativos</button>}</section></>}

function TestStudio({services,scenarios,api,onSaved}:{services:Service[];scenarios:Scenario[];api:API;onSaved:(s:Scenario)=>void}){const [open,setOpen]=useState(false);const [message,setMessage]=useState('');async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const s=await api.saveScenario({name:String(f.get('name')),serviceRef:String(f.get('service')),environment:String(f.get('environment')),type:'http',spec:{url:String(f.get('url')),method:'GET',timeout:String(f.get('timeout')),assertions:[{type:'status_exact',expected:Number(f.get('expectedStatus'))},{type:'latency_max_ms',maxMs:Number(f.get('maxMs'))}],headers:{'X-Synthetic-Test':'sentinelops'}}});onSaved(s);setOpen(false);setMessage('Cenário HTTP publicado. A execução permanece inconclusiva até que o runner alcance um alvo autorizado.')}catch(err){setMessage(String(err))}}
  return <><div className="page-head"><div><p className="eyebrow">TEST STUDIO</p><h1>Testes que explicam a falha</h1><p>Crie sinais sintéticos seguros sem começar por código.</p></div><button className="primary compact" onClick={()=>setOpen(true)}><Beaker size={18}/>Novo cenário</button></div>{message&&<div className="notice">{message}</div>}<div className="scenario-grid">{scenarios.map(s=><article className="scenario-card" key={s.id}><div><span className="type">{s.type}</span><span className="version">v{s.version}</span></div><h2>{s.name}</h2><p>{s.serviceRef} · {s.environment}</p><div className="scenario-status"><span className="pulse"/>Configuração ativa; consulte a evidência da execução antes de aprovar</div></article>)}{!scenarios.length&&<Empty text="Publique o primeiro cenário para iniciar o monitoramento sintético."/>}</div>{open&&<div className="modal-backdrop" onMouseDown={()=>setOpen(false)}><form className="modal" onSubmit={save} onMouseDown={e=>e.stopPropagation()}><p className="eyebrow">WIZARD · ETAPA ÚNICA</p><h2>Novo teste HTTP</h2><div className="form-grid"><Field label="Nome" help="Identificador DNS-safe. Ex.: api-health"><input name="name" required pattern="[a-z0-9][a-z0-9-]{1,62}"/></Field><Field label="Serviço" help="Associa evidências e ownership."><select name="service" required>{services.map(s=><option key={s.id} value={s.name}>{s.displayName}</option>)}</select></Field><Field label="Ambiente" help="Evita misturar baseline de DEV e PRD."><select name="environment"><option>development</option><option>staging</option><option>production</option></select></Field><Field label="Tipo" help="Somente HTTP possui runner distribuído habilitado."><input value="HTTP" readOnly/></Field><Field label="Endpoint" help="Hostname e IP efetivo devem constar na allowlist do runner."><input name="url" type="url" required placeholder="https://app.example.com/health"/></Field><Field label="Status esperado" help="Código exato; HTTP 200 não é sucesso sozinho."><input name="expectedStatus" type="number" min="100" max="599" defaultValue="200" required/></Field><Field label="Latência máxima (ms)" help="Orçamento observado em cada execução."><input name="maxMs" type="number" min="1" max="120000" defaultValue="10000" required/></Field><Field label="Timeout" help="Limite total; recomendado: 10s."><input name="timeout" defaultValue="10s" required/></Field></div><div className="modal-actions"><button type="button" onClick={()=>setOpen(false)}>Cancelar</button><button className="primary">Publicar cenário<ChevronRight size={18}/></button></div></form></div>}</>}
function Field({label,help,children}:{label:string;help:string;children:React.ReactNode}){return <label className="field"><span>{label}</span>{children}<small>{help}</small></label>}
function ReleaseView(){return <><div className="page-head"><div><p className="eyebrow">DELIVERY ASSURANCE</p><h1>Validação pós-deploy</h1><p>Checks versionados; ausência de evidência nunca vira sucesso.</p></div></div><section className="panel"><PanelTitle icon={Rocket} title="Nenhuma release selecionada" subtitle="A tela ainda não consulta validações vinculadas a commit, digest e janela."/><Empty text="Sem evidência de release. Resultado do gate permanece INCONCLUSIVE até a consulta de dados reais."/></section></>}
function Agents({agents}:{agents:Agent[]}){return <><div className="page-head"><div><p className="eyebrow">AGENT FLEET</p><h1>Execução onde o sistema vive</h1><p>Localidade, capabilities e heartbeat sem abrir a rede privada.</p></div></div><div className="agent-table"><div className="table-head"><span>Agente</span><span>Localidade</span><span>Ambiente</span><span>Heartbeat</span><span>Status</span></div>{agents.map(a=><div className="table-row" key={a.id}><b>{a.name}</b><span>{a.location||a.region}</span><span>{a.environment}</span><span>{a.lastHeartbeat?new Date(a.lastHeartbeat).toLocaleString():'Nunca'}</span><strong className={a.status}><i/>{a.status}</strong></div>)}{!agents.length&&<Empty text="Nenhum agente registrado."/>}</div></>}
function DataGovernance({api}:{api:API}){const [items,setItems]=useState<DataLifecycleRequest[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const [message,setMessage]=useState('');const [domains,setDomains]=useState<DataLifecycleDomain[]>(['control-plane-metadata']);const domainOptions:DataLifecycleDomain[]=['control-plane-metadata','operational-evidence','telemetry-references'];async function refresh(){setLoading(true);try{setItems(await api.dataLifecycleRequests());setError('')}catch(err){setError(String(err))}finally{setLoading(false)}}useEffect(()=>{void refresh()},[api]);function toggleDomain(domain:DataLifecycleDomain){setDomains(current=>current.includes(domain)?current.filter(item=>item!==domain):[...current,domain])}async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);if(!domains.length){setError('Selecione pelo menos um domínio.');return}try{const created=await api.createDataLifecycleRequest({requestType:String(form.get('requestType')) as 'export'|'erasure',reason:String(form.get('reason')).trim(),scope:{subjectRef:String(form.get('subjectRef')).trim(),domains}});setItems(current=>[created,...current]);setMessage('Solicitação registrada e auditada. Uma segunda pessoa com Platform Administrator deve aprová-la.');setError('');e.currentTarget.reset();setDomains(['control-plane-metadata'])}catch(err){setError(String(err))}}async function approve(id:string){try{const updated=await api.approveDataLifecycleRequest(id);setItems(current=>current.map(item=>item.id===id?updated:item));setMessage('Solicitação aprovada. Nenhuma exportação ou exclusão foi iniciada.');setError('')}catch(err){setError(String(err))}}async function execute(item:DataLifecycleRequest){if(!window.confirm(`Confirmar exclusão do perfil e vínculos RBAC de ${item.scope.subjectRef}? Esta ação não pode ser desfeita.`))return;try{const updated=await api.executeDataLifecycleRequest(item.id);setItems(current=>current.map(entry=>entry.id===item.id?updated:entry));setMessage('Erasure de identidade do control plane concluído. Outros domínios não foram alterados.');setError('')}catch(err){setError(String(err))}}function executable(item:DataLifecycleRequest){return item.status==='approved'&&item.requestType==='erasure'&&item.scope.domains.length===1&&item.scope.domains[0]==='control-plane-metadata'}return <><div className="page-head"><div><p className="eyebrow">DATA GOVERNANCE</p><h1>Solicitações de ciclo de vida</h1><p>Fluxo de quatro olhos para exportação ou exclusão. Apenas erasure de identidade do control plane possui executor.</p></div></div><div className="notice">Use uma referência pseudônima, não e-mail, nome ou conteúdo do titular. Esta tela é exclusiva de Platform Administrator; a API reaplica a autorização.</div>{error&&<div className="error">{error}</div>}{message&&<div className="notice">{message}</div>}<section className="panel"><PanelTitle icon={FileArchive} title="Nova solicitação" subtitle="Escopo limitado e auditado; exportação e outros backends ainda não têm executor."/><form className="form-grid" onSubmit={submit}><Field label="Operação" help="Exportar ou apagar após política e executor homologados."><select name="requestType" defaultValue="export"><option value="export">Exportação</option><option value="erasure">Exclusão</option></select></Field><Field label="Referência pseudônima" help="ID estável sem e-mail, espaços ou dados pessoais brutos."><input name="subjectRef" required pattern="[A-Za-z0-9][A-Za-z0-9._:-]{0,127}" maxLength={128} placeholder="employee-opaque-123"/></Field><Field label="Motivo" help="Registro operacional conciso, até 1.024 caracteres."><textarea name="reason" required maxLength={1024} rows={3}/></Field><fieldset className="field"><span>Domínios</span><div className="checkboxes">{domainOptions.map(domain=><label key={domain}><input type="checkbox" checked={domains.includes(domain)} onChange={()=>toggleDomain(domain)}/>{domain}</label>)}</div><small>Selecione apenas fontes aprovadas no pedido.</small></fieldset><div className="modal-actions"><button className="primary">Registrar solicitação<ChevronRight size={18}/></button></div></form></section><section className="panel"><PanelTitle icon={ShieldCheck} title="Fila auditável" subtitle="O solicitante não pode aprovar o próprio pedido; execução exige confirmação adicional."/>{loading?<div className="loading">Consultando solicitações…</div>:<div className="agent-table"><div className="table-head"><span>Tipo / referência</span><span>Escopo</span><span>Solicitante</span><span>Estado</span><span>Ação</span></div>{items.map(item=><div className="table-row" key={item.id}><div><b>{item.requestType==='export'?'Exportação':'Exclusão'}</b><br/><code>{item.scope.subjectRef}</code></div><span>{item.scope.domains.join(', ')}</span><span>{item.requestedBy}{item.approvedBy&&<> → {item.approvedBy}</>}</span><strong className={item.status}><i/>{item.status}</strong><span>{item.status==='requested'?<button className="compact" onClick={()=>void approve(item.id)}>Aprovar</button>:executable(item)?<button className="compact" onClick={()=>void execute(item)}>Executar erasure</button>:'Sem ação automática'}</span></div>)}{!items.length&&<Empty text="Nenhuma solicitação registrada."/>}</div>}</section></>}
function Help(){return <><div className="page-head"><div><p className="eyebrow">OBSERVABILITY BASICS</p><h1>Decida sem decorar jargões</h1><p>Definições curtas conectadas ao impacto operacional.</p></div></div><div className="glossary">{glossary.map(([term,definition])=><article key={term}><div className="glossary-icon"><BookOpen/></div><h2>{term}</h2><p>{definition}</p><a href="https://sre.google/sre-book/service-level-objectives/" target="_blank" rel="noreferrer">Ver contexto <ChevronRight size={15}/></a></article>)}</div></>}
function Empty({text}:{text:string}){return <div className="empty"><Boxes/><p>{text}</p></div>}
