# Drop-in opt-in para hosts Docker aprovados. Mantém o coletor principal e
# habilita logs JSON, métricas cAdvisor e APM eBPF. Beyla usa PID/rede do host
# e é uma exceção privilegiada aprovada explicitamente por host.
[Service]
Environment="COMPOSE_PROFILES=containers,cadvisor,beyla"
